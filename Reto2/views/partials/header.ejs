<header class="site-header">
  <div class="shell">
    <a href="/" class="brand">
      <span class="brand-mark" aria-hidden="true">🎬</span>
      <span class="brand-copy">
        <strong>CineVault</strong>
        <small>Tu colección personal</small>
      </span>
    </a>
    <nav class="site-nav" aria-label="Navegación principal">
      <ul>
        <li>
          <a href="/" class="<%= currentPath === '/' ? 'is-active' : '' %>">Inicio</a>
        </li>
        <% if (user) { %>
          <li>
            <a href="/peliculas" class="<%= currentPath === '/peliculas' ? 'is-active' : '' %>">Mis Películas</a>
          </li>
          <% } %>
            <li>
              <a href="/contacto" class="<%= currentPath === '/contacto' ? 'is-active' : '' %>">Contacto</a>
            </li>
            <% if (user) { %>
              <li class="user-menu">
                <span class="user-greeting">👤 <%= user.username %></span>
              </li>
              <li>
                <a href="/logout" class="btn-logout">Cerrar Sesión</a>
              </li>
              <% } else { %>
                <li>
                  <a href="/login" class="<%= currentPath === '/login' ? 'is-active' : '' %>">Login</a>
                </li>
                <% } %>
      </ul>
    </nav>
  </div>
</header>