<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head', { pageTitle }) %>

  <body>
    <%- include('../partials/header') %>
      <main class="page login shell">
        <section class="section auth auth--glass">
          <div class="auth-copy">
            <p class="eyebrow">Acceso seguro</p>
            <h1>
              <%= pageTitle %>
            </h1>
            <p class="lead">
              <%= helperText %>
            </p>
            <ol class="auth-steps">
              <li>
                <strong>1. Identifícate</strong>
                <span>Usa el usuario compartido en el onboarding.</span>
              </li>
              <li>
                <strong>2. Confirma tu rol</strong>
                <span>Verás colecciones personales o comunitarias.</span>
              </li>
              <li>
                <strong>3. Continúa donde lo dejaste</strong>
                <span>Seguimiento de préstamos y catálogos actualizados.</span>
              </li>
            </ol>
            <div class="auth-links">
              <a href="/">← Volver al inicio</a>
              <a href="/contacto">¿Necesitas ayuda?</a>
            </div>
          </div>
          <form class="form-card form-card--accent" method="POST" action="/login"
            aria-label="Formulario de inicio de sesión">
            <% if (error) { %>
              <div class="error-message">
                <strong>⚠️ Error:</strong>
                <%= error %>
              </div>
              <% } %>

                <label for="username">Nombre de usuario</label>
                <input type="text" id="username" name="username" placeholder="ej: usuario123" required />

                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" placeholder="Ingresa tu contraseña" required />

                <button type="submit" class="btn primary full">Iniciar sesión</button>
                <p class="form-hint">
                  Prueba con: usuario123 / contraseña123
                </p>
          </form>
        </section>

        <section class="section shell faq">
          <header>
            <p class="eyebrow">Preguntas frecuentes</p>
            <h2>Resuelve tus dudas antes de entrar</h2>
          </header>
          <div class="accordion">
            <% faqs.forEach(function(item, index){ %>
              <article>
                <details <%=index===0 ? 'open' : '' %>>
                  <summary>
                    <%= item.question %>
                  </summary>
                  <p>
                    <%= item.answer %>
                  </p>
                </details>
              </article>
              <% }) %>
          </div>
        </section>
      </main>
      <%- include('../partials/footer') %>
  </body>

</html>